import 'package:flutter/material.dart';
import 'package:ui_chat_app/themes/app_colors.dart';

const kBottomNavHeight = 68.0;

const kRestaurantCardShadow = BoxShadow(
  blurRadius: 18,
  spreadRadius: -3,
  offset: Offset(0, 2),
  color: AppColors.inputBorder,
);

const kVoucherCardShadow = BoxShadow(
  blurRadius: 18,
  offset: Offset(0, 2),
  spreadRadius: -12,
  color: AppColors.inputBorder,
);
